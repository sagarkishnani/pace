name = "pace-correos"
main = "src/index.ts"
compatibility_date = "2026-09-16"
compatibility_flags = ["nodejs_compat"]

# SUPABASE_URL y SUPABASE_SERVICE_ROLE_KEY van como secretos:
#   wrangler secret put SUPABASE_SERVICE_ROLE_KEY
#
# REMITENTES es opcional: direcciones extra separadas por coma.
# Si reenvías desde Gmail, tu propia dirección tiene que estar acá.
